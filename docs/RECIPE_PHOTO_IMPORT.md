# 写真取り込み

`POST /api/recipes/import-photo` は圧縮済みの画像を最大5枚受け取り、レシピ下書きを返します。

`OPENAI_API_KEY` が設定されている場合は OpenAI Vision を利用します。未設定時は安全なモック動作となり、画像内容を推測して材料や手順を作りません。画像は読み取り処理のため外部サービスへ送信される場合があるため、個人情報を含む画像は送信しないでください。
